# Server Configuration
NODE_ENV=development
PORT=3001

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/freelancer-portal

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-refresh-token-secret-key-optional

# Email Configuration (Gmail SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM_NAME=Connect - Accel
EMAIL_FROM_EMAIL=noreply@freelancerportal.com

# Frontend URL (for email links and CORS)
FRONTEND_URL=http://localhost:3000

# Redis Configuration (Optional - for caching, queues, and real-time features)
# If Redis is not available, the app will work with reduced functionality:
# - Emails sent directly (no background queue)
# - No response caching
# - Refresh tokens in memory (lost on restart)
# - Socket.IO works but won't scale across servers
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Backend URL (optional - for internal references)
BACKEND_URL=http://localhost:3001
