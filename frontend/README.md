
  # Connect-Accel Product Blueprint

  This is a code bundle for Connect-Accel Product Blueprint. The original project is available at https://www.figma.com/design/KVNtAuhbD4yV5g3ORYCuxt/Connect-Accel-Product-Blueprint.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # connect-accel
